group 'org.jetbrains.kotlinx.benchmark.examples'
version '0.1-SNAPSHOT'

subprojects {
    repositories {
        maven {
            name = 'build'
            url = "$rootProject.buildDir/maven"
        }

        mavenLocal()
        mavenCentral()
    }
}
