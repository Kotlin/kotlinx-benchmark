plugins {
    id 'java'
    id 'org.jetbrains.gradle.benchmarks.plugin' version '0.1.7' 
}

dependencies {
    implementation(project(":runtime"))
}

benchmark {
    targets {
        register("main") {
            jmhVersion = "1.21"
        }
    }
}
