import org.jetbrains.kotlin.gradle.targets.wasm.d8.D8EnvSpec

kotlin {
    wasmJs {
        d8()
    }
}

benchmark {
    targets {
        register("wasmJs")
    }
}

rootProject.extensions.getByType(D8EnvSpec).version = "12.6.99"
